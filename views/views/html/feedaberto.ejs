<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FeedPostAberto</title>
    <link rel="stylesheet" href="../css/feedaberto.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@500&display=swap" rel="stylesheet">
</head>

<body>
    <!-- Criação da NavBar-->
    <div class="navbar">
        <label for="lg">
            <div class="logo"><span><img id="img-logo" src="../img/cjr.png"></span></div>
        </label>
        <form action="/">
            <button id="lg" style="display: none;">AAAA</button>
        </form>
        <div class="navbar-direito">
            <a href="/cadastro" class="link">Criar Conta</a>
            <form action="/login">
                <button type="submit" class="button">Entrar</button>
            </form>
        </div>
    </div>

    <div class="feed">
      <% for (let i = posts.length - 1; i >= 0; i--) { %>
          <div class="tweet">
            <% if(!posts[i].User.imagem) {%>
                <img class="ImagemPerfil" src="../img\ImagemPerfil.png" alt="ImagemPerfil">
                <% }else{ %>
                    <img class="ImagemPerfil" src="<%posts[i].User.imagem %>">
                <% } %>
              <div class="username"><a href="/login"><%= posts[i].User.username %></a></div>
              <div class="timestamp">- <%= posts[i].updated_at %></div>
              <div class="content"><a href="/login"><%= posts[i].content %></a></div>
              <img class="Balao" src="../img/Balão.png" alt="Balao">
          </div>
      <% } %>        
      </div>
  